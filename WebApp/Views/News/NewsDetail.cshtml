@model LibraryModels.News
@{
    ViewData["Title"] = "NewsDetail";
    Layout = "~/Views/Shared/_Layout.cshtml"; 
}

@*<div class="site-blocks-cover overlay" data-aos="fade" data-stellar-background-ratio="0.5">
    <div class="container">
        <div class="row align-items-center justify-content-start">
            <div class="col-md-6 text-center text-md-left" data-aos="fade-up" data-aos-delay="400">
                <h1 class="bg-text-line">News</h1>
                <p class="mt-4">Read the Latest News and Updates</p>
            </div>
        </div>
    </div>
</div>*@

<div class="site-section">
    <div class="container">
        <div class="row mb-5">
            <div class="col-md-12">
                <div class="post-entry">
                    <div class="text p-4">
                        <h2 class="h3 text-black">@Model.Title</h2>
                        <span class="text-uppercase date d-block mb-3"><small>@Model.PublishDate</small></span>
                        <div class="content">
                            @Html.Raw(Model.Content)
                        </div>
                        <form>
                            <h3></h3>
                            <div class="form-group">
                                <textarea id="CommentText" name="CommentText" class="form-control" rows="3" placeholder="Write your comment here..."></textarea>
                            </div>
                            <button class="btn btn-success">Comment</button>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>






@*<div class="container">
    <h2>@Model.Title</h2>
    <p>@Model.Content</p>
    <p><strong>Publish Date:</strong> @Model.PublishDate.ToShortDateString()</p>
    
    <!-- Danh sách bình luận -->
   
    
    <!-- Biểu mẫu để thêm bình luận mới -->
    <h3>Add Comment</h3>
    <form asp-action="Write your comment" method="post">
        <div class="form-group">
            <textarea id="CommentText" name="CommentText" class="form-control" rows="3" placeholder="Write your comment here"></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Add Comment</button>
    </form>
</div>*@





@section Scripts {
    <script>
        // JavaScript để kiểm tra tính hợp lệ của biểu mẫu
        (function () {
            'use strict';

            // Lắng nghe sự kiện submit của biểu mẫu
            var form = document.querySelector('.needs-validation');
            form.addEventListener('submit', function (event) {
                if (!form.checkValidity()) {
                    event.preventDefault();
                    event.stopPropagation();
                }
                form.classList.add('was-validated');
            }, false);
        })();
    </script>
}